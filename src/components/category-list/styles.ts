import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 25,
  },
  categoryListContainer: {
    alignItems: 'center',
  },
  categoryList: {
    flex: 1,
    gap: 20,
    paddingBottom: 25,
    marginTop: 10,
  },
  columnWrapper: {
    gap: 20,
  },
})
