import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  title: {
    fontWeight: 'bold',
    fontSize: 20,
    marginBottom: 10,
  },
  categoryListContainer: {
    flex: 1,
    paddingBottom: 20,
  },
  categoryList: {
    gap: 25,
    alignItems: 'center',
  },
  columnWrapper: {
    gap: 25,
  },
})
