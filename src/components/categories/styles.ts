import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  categoryListContainer: {
    flex: 1,
    paddingBottom: 25,
  },
  categoryList: {
    flex: 1,
    gap: 25,
    alignItems: 'center',
    paddingBottom: 25,
    marginTop: 10,
  },
  columnWrapper: {
    gap: 25,
  },
})
