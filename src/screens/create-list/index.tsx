import { View } from 'react-native'
import { Typography } from 'src/components'

const CreateList = () => {
  return (
    <View>
      <Typography>CreateList</Typography>
    </View>
  )
}

export default CreateList
